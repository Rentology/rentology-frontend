export interface IUser {
    id: bigint,
    email: string,
    phone: string | null,
    name: string | null,
    lastName: string | null,
    secondName: string | null
    birthDate: string | null
    sex: string | null

}