import React, {FC} from 'react';
import Navbar from "../../ui/navbar/Navbar";

const Profile: FC = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default Profile;